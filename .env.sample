# Host for the FastAPI app (default: 127.0.0.1 for local dev)
APP_HOST=127.0.0.1

# Port for the FastAPI app (default: 8000)
APP_PORT=8000

# Redis connection URL (e.g., redis://localhost:6379/0)
REDIS_URL=

# PostgreSQL DATABASE settings

DATABASE_NAME=
DATABASE_USER=
DATABASE_PASSWORD=
DATABASE_HOST=
DATABASE_PORT=5432

# Logging level (default: INFO; options: DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO
